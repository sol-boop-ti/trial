# VideoGrid

The five-column proof grid (`grid-columns`, `gap-grid` gutters) that holds VideoCards.

- Provide: VideoCard children; give the first `span={2}`.
- At 1470px wide the columns are 267px and the lead tile 544px. Row height here is 306px (the lead tile at 16:9); the live row height was not captured.
- Order tiles so every row mixes verticals, and lead with the strongest customer name.
