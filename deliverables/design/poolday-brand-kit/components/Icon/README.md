# Icon

A Lucide line icon drawn inline at 16px with a 2px stroke, coloured by `currentColor`.

- Provide: `name` (layout-grid, briefcase, gamepad-2, mic, chevron-down, volume-2), optional `size`.
- Set colour on the parent text: `ink` when active, `ink-muted` otherwise.
