# L3: "Claude Opus 5.5 + Poolday = Infinite UGC farm" (LinkedIn post)

Record your screen through steps 1 and 2 (Cmd+Shift+5 → Record entire screen). Keep the menu-bar clock visible.

## Step 1: Claude writes the scripts (claude.ai, Opus 5.5)
```
Write 8 UGC video scripts for [PRODUCT + URL], 15–20 seconds each, for
Instagram Reels and TikTok.
4 creators, 2 scripts each: a founder at a desk, a student on a couch, a
runner outdoors, a designer in a studio.
Each script: a hook in the first 2 seconds, one clear benefit, a natural
spoken tone, and a short call to action. 8 different angles: pain point,
before/after, "I tried it for a week", reaction, tutorial, myth-busting,
comparison, story.
Format: a numbered list. For each: creator, hook, script, on-screen text.
```

## Step 2: Poolday makes the reels (Align mode, Ultra tier)
Paste Claude's 8 scripts under this:
```
Here are 8 UGC scripts for [PRODUCT + URL].
1. Build the brand kit from the URL if it doesn't exist.
2. Create the 4 AI creators described in the scripts and save them so I
   can reuse them.
3. Make the 8 reels: 9:16, 15–20s, captions on, a light end card with the
   logo. Mark each as AI-generated in its caption.
4. Save this as a command "/ugc-batch" so next week I just paste new scripts.
Validation: first show me the 4 creators on one board; I approve, then build.
```
Note the real time, the number of prompts and the credits used.

## Step 3: the 15s demo (new Poolday conversation)
Upload the screen recording and the 8 reels, then paste:
```
Attached: a screen recording of Claude writing 8 UGC scripts and Poolday
turning them into reels, plus the 8 reels.
Make a 15s LinkedIn video, 4:5, sound off:
0–2s: a 4x2 grid of the 8 reels playing, big text
"Claude Opus 5.5 + Poolday = Infinite UGC farm".
2–5s: Claude writing the scripts, sped up.
5–12s: Poolday building the reels, sped up 20–40x: the creators appear,
then the grid fills as each reel finishes. A small real-time clock in the corner.
12–15s: the full grid again, text "[N] reels · [T]".
Keep the UI readable: zoom into the part that matters in each cut.
Validation: show me the cut list first.
```
